# CSCI316 group project — loan default risk analysis
#
# Tested with Python 3.11 / 3.12. PySpark and TensorFlow do not yet publish
# wheels for Python 3.14, so use a 3.11 or 3.12 interpreter.
#
# Stage 1 and Stage 2 (Process One) additionally require a JDK (11 or 17) on PATH.

pyspark==4.0.0
tensorflow==2.19.0
scikit-learn==1.6.1
pandas==2.2.3
numpy==2.1.3
matplotlib==3.10.0
pyarrow==18.1.0
